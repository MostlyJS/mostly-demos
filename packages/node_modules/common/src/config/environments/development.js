import path from 'path';

export default {
  mongodb: {
    db: 'mongodb://127.0.0.1/mostly-demos',
    dbName: 'mostly-demos'
  },

  nats: {
    severs: ['nats://127.0.0.1:4222']
  },

  mongoose: {
    schemaConfig: {
      autoIndex: true
    },
  },

  paginate: {
    default: 10,
    max: 50
  },

  redis: {
    port: 6379,
    host: '127.0.0.1',
    options: {
    },
    cache_related: {
    }
  },

  elasticSearch: {
    'host': '127.0.0.1:9200',
    'log': 'trace'
  }
  
};
