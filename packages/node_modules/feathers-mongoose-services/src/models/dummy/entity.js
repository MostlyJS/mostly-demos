import Entity from 'mostly-entity';

const DummyEntity = new Entity('Dummy', {
  _id: { as: 'id', type: 'string' }
});

DummyEntity.excepts('_id', 'createdAt', 'updatedAt', 'destroyedAt');

export default DummyEntity.asImmutable();
