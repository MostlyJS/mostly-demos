import { config } from 'common';
import dummy from './dummy/dummy.service';

export default function (app) {
  const options = {
    paginate: config.paginate
  };

  // initialize service
  app.use('dummies', dummy(app, options));

}
