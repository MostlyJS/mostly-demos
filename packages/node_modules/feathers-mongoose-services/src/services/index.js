const { config } = require('common');
const dummy = require('./dummy/dummy.service');

module.exports = function (app) {
  const options = {
    paginate: config.paginate
  };

  // initialize service
  app.use('dummies', dummy(app, options));
};
